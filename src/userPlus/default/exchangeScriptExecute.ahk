

; 普通行われる処理の変わりに実行する処理を記入する。
; 返り値が True なら、普通行われる処理を実行せずスルーする。
; 返り値が False なら、普通行われる処理が実行される。
; plusOption には ６つめのオプションに指定した文字が入ってる
userpese_exchangeScriptExecute(file, args, workFolder, option5, plusOption, count){

/*
	if( plusOption == 1 ){
		userMakeFunction(file)

		startup_output(count . " つめの処理を無事実行`n")
		return True
	}

*/


	return False
}
